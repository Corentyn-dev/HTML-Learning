HTML Exercises

Exercise 1: HTML Page Structure

Create a basic structure for an HTML page using the <html>, <head>, <title>, and <body> tags.

Exercise 2: Content Markup

Use appropriate HTML tags to create a paragraph of text (<p>), a level 1 heading (<h1>), and a hyperlink (<a>) with the href attribute.

Exercise 3: Creating Hyperlinks

Add hyperlinks to external pages using the <a> tag with the href attribute. Verify that the links function correctly.
